import { it, test } from 'vitest'

// test -> it
// it -> BDD 行为驱动开发 TDD
// it should xxx xxx，（Mocha Jasmine等框架）
// 两个都支持 喜欢哪个用哪个

test('should', () => {})
it('should', () => {})

// 待实现测试
test.todo('todo', () => {})
